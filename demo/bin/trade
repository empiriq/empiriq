#!/usr/bin/env php
<?php

use App\Kernel;
use Empiriq\Contracts\Runner;

require dirname(__DIR__, 2) . '/vendor/autoload.php';


$kernel = new Kernel('trade', true);
$kernel->boot();

/** @var Runner $entrypoint */
$entrypoint = $kernel->getContainer()->get(Runner::class);
$entrypoint->run();
